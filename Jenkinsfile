pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
               sh 'pwd'
               sh 'aws sts get-caller-idenity'
              
            }
        }
    }
}